create database student_management;
use student_management;

create table Class(
	id int,
    name varchar(200)
);

create table Teacher(
	id int,
    name varchar(200),
    age int,
    contry varchar(50)
);
